# Windows batch and script files must keep CRLF or cmd.exe mis-parses them.
*.bat text eol=crlf
*.vbs text eol=crlf
*.ps1 text eol=crlf

# Generated by tools/make-icon.mjs. Binary, so no line-ending translation
# can corrupt it in transit through a checkout.
*.ico binary
